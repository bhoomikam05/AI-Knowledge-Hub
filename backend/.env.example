# Groq API Key (Used for LLM Summarization, Q&A, and Speech-to-Text Fallback)
GROQ_API_KEY=your_groq_api_key_here

# Optional: YouTube Proxy for Cloud Deployments (Render / AWS / GCP) to prevent IP blocking
# YOUTUBE_PROXY=http://username:password@proxy_host:port